{
  // Use the project's TypeScript version so Astro's TS plugin loads correctly
  "typescript.tsdk": "node_modules/typescript/lib",
  // Nice to have: let the Astro extension format .astro files
  "[astro]": { "editor.defaultFormatter": "astro-build.astro-vscode" }
}

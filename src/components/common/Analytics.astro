---
/**
 * Vercel Analytics for Astro (default import).
 * Alias the import to avoid a name collision with this file’s implicit component name.
 * No visual output; safe to include on every page.
 */
import VercelAnalytics from '@vercel/analytics/astro';
---

<VercelAnalytics />

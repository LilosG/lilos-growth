---
import TestimonialBand from "~/components/results/TestimonialBand.astro";
import type { Testimonial } from "~/data/results";

interface Props {
  items: Testimonial[];
}

const { items } = Astro.props as Props;
---

<TestimonialBand items={items} />

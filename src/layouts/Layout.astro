---
/**
 * Site page layout — loads Tailwind, meta components, and site-wide scripts.
 * Titles/descriptions are centralized here via <Metadata />, so pages
 * can simply `export const title/description` (optional).
 */

import '~/assets/styles/tailwind.css';

import CommonMeta from '~/components/common/CommonMeta.astro';
import Favicons from '~/components/Favicons.astro';
import CustomStyles from '~/components/CustomStyles.astro';
import ApplyColorMode from '~/components/common/ApplyColorMode.astro';
import Metadata from '~/components/common/Metadata.astro';
import SiteVerification from '~/components/common/SiteVerification.astro';
import Analytics from '~/components/common/Analytics.astro';
import BasicScripts from '~/components/common/BasicScripts.astro';
import SpeedInsights from '@vercel/speed-insights/astro';

import type { MetaData as MetaDataType } from '~/types';

declare global {
  interface Window {
    trackEvent?: (name: string, data?: Record<string, unknown>) => void;
    va?: (type: string, payload: Record<string, unknown>) => void;
  }
}

/** Allow pages to pass minimal meta (typed; no `any`) */
type MetaInput = Partial<MetaDataType> & {
  title?: string;
  description?: string;
};

export interface Props {
  title?: string;
  description?: string;
  lang?: string;
  bodyClass?: string;
  metadata?: MetaInput;
}

const { title, description, lang = 'en', bodyClass = '', metadata = {} } = Astro.props as Props;

/** Fallback titles for routes that were missing one (prevents duplicates) */
const pathname = Astro.url.pathname;
let fallbackTitle: string | undefined;

if (!title) {
  if (pathname === '/' || pathname === '/index') {
    // Home
    fallbackTitle = 'Local SEO & Websites for Home Service Pros';
  } else if (pathname.startsWith('/additional-services')) {
    fallbackTitle = 'Additional Services for Home Service Businesses';
  }
}
const effectiveTitle = title ?? fallbackTitle;
---

<!doctype html>
<html lang={lang} dir="ltr" class="h-full">
  <head>
    <CommonMeta />
    <Favicons />
    <CustomStyles />
    <ApplyColorMode />
    <!-- Centralized SEO: emits <title>, meta description, canonical, OG/Twitter, robots, etc. -->
    <Metadata {...metadata} title={effectiveTitle} description={description} />
    <SiteVerification />
    <Analytics />
  </head>

  <body
    class={`antialiased text-default bg-page tracking-tight min-h-screen min-h-[100dvh] flex flex-col overflow-x-clip ${bodyClass}`}
  >
    <div class="flex-1 flex flex-col">
      <slot />
    </div>

    <BasicScripts />
    <SpeedInsights />

    <script is:inline>
      window.trackEvent = (name, data = {}) => {
        try {
          if (typeof window.va === 'function') {
            window.va('event', { name: String(name), data });
          }
        } catch {}
      };
    </script>
  </body>
</html>

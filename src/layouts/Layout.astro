---
import '~/assets/styles/tailwind.css';

import CommonMeta from '~/components/common/CommonMeta.astro';
import Favicons from '~/components/Favicons.astro';
import CustomStyles from '~/components/CustomStyles.astro';
import ApplyColorMode from '~/components/common/ApplyColorMode.astro';
import Metadata from '~/components/common/Metadata.astro';
import SiteVerification from '~/components/common/SiteVerification.astro';
import Analytics from '~/components/common/Analytics.astro';
import BasicScripts from '~/components/common/BasicScripts.astro';

// Vercel Web Analytics (aliased to avoid name conflict)
import VercelAnalytics from '@vercel/analytics/astro';
// Vercel Speed Insights
import SpeedInsights from '@vercel/speed-insights/astro';

// Comment the line below to disable View Transitions
import { ClientRouter } from 'astro:transitions';

// Floating Calendly Button
import FloatingCalendlyButton from '../components/FloatingCalendlyButton.astro';

import type { MetaData as MetaDataType } from '~/types';

export interface Props {
  metadata?: MetaDataType;
}

const { metadata = {} } = Astro.props;
---

<!doctype html>
<html lang="en" dir="ltr" class="2xl:text-[20px]">
  <head>
    <CommonMeta />
    <Favicons />
    <CustomStyles />
    <ApplyColorMode />
    <Metadata {...metadata} />
    <SiteVerification />
    <Analytics />

    <!-- Comment the line below to disable View Transitions -->
    <ClientRouter fallback="swap" />
  </head>

  <body class="antialiased text-default bg-page tracking-tight">
    <slot />

    <!-- Floating Calendly Button (shows on all pages) -->
    <FloatingCalendlyButton />

    <BasicScripts />

    <!-- Vercel Web Analytics -->
    <VercelAnalytics />

    <!-- Vercel Speed Insights -->
    <SpeedInsights />

    <!-- Global custom analytics helper (inline JS so no tooling errors) -->
    <script is:inline>
      // Call anywhere: window.trackEvent("Book a Call", { location: "hero" })
      window.trackEvent = (name, data) => {
        try {
          if (window.va && typeof window.va.track === 'function') {
            window.va.track(String(name), data || {});
          }
        } catch {
          // no-op: analytics failures shouldn't break UX
        }
      };
    </script>
  </body>
</html>

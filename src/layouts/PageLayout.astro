---
import '~/assets/styles/tailwind.css';

import CommonMeta from '~/components/common/CommonMeta.astro';
import Favicons from '~/components/Favicons.astro';
import CustomStyles from '~/components/CustomStyles.astro';
import ApplyColorMode from '~/components/common/ApplyColorMode.astro';
import Metadata from '~/components/common/Metadata.astro';
import SiteVerification from '~/components/common/SiteVerification.astro';
import Analytics from '~/components/common/Analytics.astro';
import VercelAnalytics from '@vercel/analytics/astro';
import SpeedInsights from '@vercel/speed-insights/astro';
import { ClientRouter } from 'astro:transitions';

// NOTE: your header lives here:
import Header from '~/components/widgets/Header.astro';

import FloatingCalendlyButton from '~/components/FloatingCalendlyButton.astro';
import type { MetaData as MetaDataType } from '~/types';

export interface Props {
  metadata?: MetaDataType;
}
const { metadata = {} } = Astro.props;
---

<!doctype html>
<html lang="en" dir="ltr" class="h-full 2xl:text-[20px]">
  <head>
    <CommonMeta />
    <Favicons />
    <CustomStyles />
    <ApplyColorMode />
    <Metadata {...metadata} />
    <SiteVerification />
    <Analytics />
    <ClientRouter fallback="swap" />
  </head>

  <body
    class="antialiased text-default bg-page tracking-tight min-h-screen min-h-[100dvh] flex flex-col overflow-x-clip"
  >
    <!-- Header OUTSIDE the scaled main so mobile drawer fills the screen -->
    <Header />

    <main id="page" class="flex-1 flex flex-col">
      <!-- visual downscale without affecting document height -->
      <div
        class="w-full origin-top will-change-transform md:scale-[.90] xl:scale-[.87] 2xl:scale-[.85]"
        style="transform-origin: top center;"
      >
        <slot />
      </div>
    </main>

    <FloatingCalendlyButton />

    <VercelAnalytics />
    <SpeedInsights />
    <script is:inline>
      // helper for custom analytics; no empty catch/try
      window.trackEvent = (name, data) => {
        const t = window?.va?.track;
        if (typeof t === 'function') t(String(name), data || {});
      };
    </script>
  </body>
</html>

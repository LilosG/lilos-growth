---
import '~/assets/styles/tailwind.css';

import CommonMeta from '~/components/common/CommonMeta.astro';
import Favicons from '~/components/Favicons.astro';
import CustomStyles from '~/components/CustomStyles.astro';
import ApplyColorMode from '~/components/common/ApplyColorMode.astro';
import Metadata from '~/components/common/Metadata.astro';
import SiteVerification from '~/components/common/SiteVerification.astro';
import Analytics from '~/components/common/Analytics.astro';
import BasicScripts from '~/components/common/BasicScripts.astro';

// View Transitions (optional)
import { ClientRouter } from 'astro:transitions';

// Global header
import Header from '~/components/widgets/Header.astro';

// Floating Calendly Button
import FloatingCalendlyButton from '~/components/FloatingCalendlyButton.astro';

import type { MetaData as MetaDataType } from '~/types';

export interface Props {
  metadata?: MetaDataType;
  title?: string;
  description?: string;
}

const { metadata = {}, title, description } = Astro.props as Props;
---

<!doctype html>
<html lang="en" dir="ltr" class="h-full">
  <head>
    <CommonMeta />
    <Favicons />
    <CustomStyles />
    <ApplyColorMode />
    <Metadata {...metadata} />
    <SiteVerification />
    <Analytics />
    <ClientRouter fallback="swap" />

    <!-- Meta title + description -->
    <title>{title ?? metadata.title ?? 'Lilos Growth'}</title>
    <meta
      name="description"
      content={description ?? metadata.description ?? 'Local SEO, websites, and Google Business Profile optimization for home service businesses.'}
    />
  </head>

  <!-- Sticky footer layout -->
  <body class="antialiased text-default bg-page tracking-tight min-h-[100dvh] flex flex-col overflow-x-clip">
    <!-- Header stays full-size -->
    <Header />

    <!-- CONTENT SCALE ONLY (typography), spacing/layout unaffected -->
    <main id="app" class="flex-1 flex flex-col text-[95%] md:text-[88%] xl:text-[84%] 2xl:text-[82%]">
      <slot />
    </main>

    <FloatingCalendlyButton />

    <BasicScripts />

    <!-- Vercel Web Analytics + Speed Insights -->
    <script is:inline type="module">
      import { inject } from '@vercel/analytics';
      import { injectSpeedInsights } from '@vercel/speed-insights';
      try {
        inject();
      } catch {}
      try {
        injectSpeedInsights();
      } catch {}
    </script>

    <!-- Plain JS analytics helper -->
    <script is:inline>
      (function () {
        if (typeof window['trackEvent'] !== 'function') {
          window['trackEvent'] = function (name, data) {
            try {
              if (window['va'] && typeof window['va'].track === 'function') {
                window['va'].track(String(name), data || {});
              }
            } catch {}
          };
        }
      })();
    </script>
  </body>
</html>
